# Lab 1.3 — Math Utils Starter

Split `math_demo.c` into:

- `math_utils.h` — public prototypes with header guard
- `math_utils.c` — implementations
- `main.c` — demo program

Add a `Makefile` with `all` and `clean` targets.

See [Module 1.3](../../phase-01-programming-foundations/module-01-03-functions/README.md).
