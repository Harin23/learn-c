# Lab 4.5 — Multi-binary Makefile

Build three small tools from shared `util.c`. Complete the Makefile.

```
makefile_lab/
├── Makefile
├── util.c / util.h
├── mycat.c
├── wc_lines.c
└── echo_file.c
```

Targets: `mycat`, `wc_lines`, `echo_file`, `clean`, `all`

See [Module 4.5](../../../part-04-linux-user-space/module-04-05-build-systems/README.md).
