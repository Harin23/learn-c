# Worksheet 8.3 — Kernel Coding Style Lab

Paper exercise — fix style problems in the **bad hunks** below. Reference `Documentation/process/coding-style.rst`.

## Part 1 — Bad hunk A

```c
int foo(int a,int b){
if(a>b)
printk("a bigger\n");
return a;
}
```

Rewrite with correct kernel style (tabs for indent, braces, spaces):

```
(your rewrite)
```

## Part 2 — Bad hunk B

```c
if (x == NULL)
    return -EINVAL;
```

Kernel prefers `NULL` check style: _______________ (rewrite line)

## Part 3 — Line length

This line is too long for kernel style (80 cols). Break or refactor conceptually:

```c
printk(KERN_INFO "learn-c: pid=%d comm=%s file=%s line=%d func=%s\n", pid, comm, file, line, func);
```

Your approach (describe or rewrite shorter): _______________

## Part 4 — Commit message

Bad message:

```
fixed stuff
```

Good subject line (≤72 chars, imperative): _______________

Body (what/why, 2 lines): _______________

## Part 5 — Signed-off-by

Write your DCO line:

```
Signed-off-by: 
```

## Part 6 — checkpatch

After your Project 44 patch, record:

- Warnings before fix: _______________
- Warnings after fix: _______________
- One warning you intentionally kept (if any) and why: _______________

## Part 7 — Patch review

Three items from [kernel-patch-review-checklist.md](../checklists/kernel-patch-review-checklist.md) most relevant to a debug printk patch:

1. _______________
2. _______________
3. _______________
