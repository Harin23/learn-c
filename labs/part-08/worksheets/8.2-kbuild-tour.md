# Worksheet 8.2 — Kbuild Tour

Kernel tree path (`KERNEL_SRC`): _______________

Pinned tag (see resources/part-08.md): _______________

## Part 1 — First build commands

Record exact commands and wall-clock time:

```bash
# defconfig
# merge_config (if used)
# build target
```

Build time (minutes): _______________

## Part 2 — Trace CONFIG_FTRACE

Fill the chain for `CONFIG_FTRACE`:

| Step | Location | What you found |
|------|----------|----------------|
| Kconfig symbol | file: | prompt text: |
| Default value | | |
| Selected in `.config` | line: | y/n/m |
| Makefile reference | file: | variable/target: |

## Part 3 — Artifacts

After successful build, record paths and sizes:

| Artifact | Path | Size (approx) |
|----------|------|---------------|
| bzImage | | |
| vmlinux | | |
| System.map | | |

## Part 4 — menuconfig

Change **one** symbol via `make menuconfig` (not in learn-c-base fragment). Document:

- Symbol name: _______________
- Why you changed it: _______________
- `grep` line in `.config`: _______________

## Part 5 — LOCALVERSION

What does `uname -r` show in VM after boot with `CONFIG_LOCALVERSION="-learn-c"`?

Expected pattern: _______________

## Part 6 — Project 43 prep

List three packages you verified with setup-kernel-build.sh:

1. _______________
2. _______________
3. _______________
