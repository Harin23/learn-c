# Worksheet 8.4 — MM and Scheduler Kernel Tour

Kernel version: _______________

## Part 1 — Scheduling source

Locate and record paths:

| Symbol / topic | File path |
|----------------|-----------|
| `context_switch` (or wrapper) | |
| CFS pick next task (`pick_next_task_fair` or similar) | |
| `sched_switch` tracepoint definition | |
| `task_struct` definition | |

## Part 2 — Memory source

| Symbol / topic | File path |
|----------------|-----------|
| `handle_mm_fault` (or fault entry) | |
| `do_mmap` or `vm_mmap` | |
| `vm_area_struct` definition | |
| `/proc/*/maps` seq_show handler | |

## Part 3 — Phase 6 tie-in

From Phase 6 Project 36, which metric does **not** apply directly to CFS?

- [ ] turnaround time
- [ ] vruntime ordering
- [ ] fixed quantum for all tasks
- [ ] waiting time in a simplified queue

Explain in one sentence: _______________

## Part 4 — ftrace commands

Write exact commands you used for sched trace:

```bash
# enable
# run workload
# read trace
# disable
```

## Part 5 — Sample trace line

Paste one `sched_switch` line and label fields (prev_comm, next_comm, CPU, etc.):

```
(paste)
```

## Part 6 — Project 45 track

Which track will you use?

- [ ] Track A — scheduling
- [ ] Track B — memory

Primary Phase 6 project to compare: _______________
