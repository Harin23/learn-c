# Worksheet 7.1 — mini-util Orientation

Complete while exploring [mini-util](../mini-util/).

## Part 1 — Build and run

1. Run `make && make test`. Which tests fail? Why (read ISSUES.md)?
2. List all `.c` files under `src/`.
3. What does `MU_BUF_SIZE` control?

## Part 2 — Search commands

Run and paste one interesting line of output for each:

```bash
rg 'count_words' .
rg 'BUG' .
git log --oneline 2>/dev/null || echo '(init repo with git init for blame practice)'
```

## Part 3 — Call graph

Draw call graph for `mu-wc` from `main` to `count_words`, `count_lines`, `count_chars`.

## Part 4 — Git blame (after `git init` in mini-util)

```bash
cd mini-util && git init && git add . && git commit -m "initial import"
git blame src/mu_wc.c | head -20
```

Which lines belong to the intentional bug?

## Part 5 — Reflection

What would you read first when joining a 500k-line fork of this project?
