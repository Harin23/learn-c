# Worksheet 7.2 — Linux Kernel Tree Tour

Use a local shallow clone or [Elixir Bootlin](https://elixir.bootlin.com/linux/latest/source).

Record kernel version: _______________

## Part 1 — Top-level directories

Fill in one-line purpose for each:

| Directory | Purpose |
|-----------|---------|
| `arch/` | |
| `kernel/` | |
| `mm/` | |
| `fs/` | |
| `drivers/` | |
| `include/` | |
| `Documentation/` | |

## Part 2 — Syscall trace (`read`)

Complete the table (file paths for your kernel version):

| Step | Function | File |
|------|----------|------|
| 1 | libc `read` | (glibc — optional) |
| 2 | syscall entry | |
| 3 | ksys/read wrapper | |
| 4 | vfs_read | |
| 5 | file ops read_iter | |

Hints: search `SYSCALL_DEFINE.*read`, `ksys_read`, `vfs_read`.

## Part 3 — MAINTAINERS

Find MAINTAINERS entry for `fs/` or ext4. Paste mailing list line.

## Part 4 — Documentation

Pick one file under `Documentation/filesystems/` or `Documentation/core-api/`. Title and 3-sentence summary.

## Part 5 — Drivers preview

List three subdirectories under `drivers/gpu/drm/` (AMDGPU lives here — Phase 10 preview).
